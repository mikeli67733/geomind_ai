{
  "server_url": "",
  "_note": "Local server URL override. Read by core/config.py Settings._local_file_value('server_url') in the resolution chain: QSettings override > this file > remote config > FALLBACK_SERVER_URL. Leave empty to use the built-in resolution chain."
}
